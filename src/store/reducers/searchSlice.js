import { createSlice } from "@reduxjs/toolkit";

const searchSlice = createSlice({
    name:"search",
    initialState:{
    },
    reducers:{
    }
})
// export const { setShowSearchModal }= searchSlice.actions;
export default searchSlice.reducer;